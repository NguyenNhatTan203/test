export const SCHEDULE = {
    STATUS: {
        ACTIVE: 'active',
        INACTIVE: 'inactive',
        CANCELLED: 'cancelled',
    }
}

export enum SEAT_STATUS {
    BOOKED = 'booked',
    RESERVED = 'reserved',
    AVAILABLE = 'available',
}

export enum TYPE_PICKUP_DROP_OFF {
    PICKUP = 'pickup',
    DROP_OFF = 'drop_off'
}

export enum STATUS {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    DELETED = 'deleted',
}